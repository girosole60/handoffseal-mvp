# HandoffSeal 免费试点操作卡

## 给试用者的承诺

- 不收费、不注册、不上传文件；
- Python 标准库即可运行；
- 原始交付文件只在试用者自己的电脑上读取；
- 输出 HTML/JSON 证据，不改原文件。

## 适合谁

每周或每月把多份 CSV/XLSX 打成一个 ZIP，交给客户、供应商或下游系统的报表、运营、外包交付团队。单独打开一张表的人，不是目标用户。

## 试点步骤

0. 先用 `run.command` 跑 demo，确认本机已有 `python3`；真实批次仍在自己的电脑上运行。
1. 只定向邀请 3–5 个高度匹配对象：周期性交付多文件 CSV/XLSX，且确实有核对、返工或解释成本。
2. 试用者选一个已经存在的重复交付批次，先不要上传或发送给任何第三方。
3. 按 `manifest.demo.json` 写一份只描述文件名、列名、主键和跨文件关系的规则。
4. 首批可以陪跑配置和解释规则，运行仍在试用者自己的电脑上完成；记录真实发现及其后果。
5. 下一批只提供文档和问答，不代操作、不代运行；这批才用于判断独立采用。
6. 记录每个发现：是否真实存在、是否会造成退回/返工/解释成本、修复后是否再次运行、是否愿意继续下一批。
7. 使用 [PILOT_LOG_TEMPLATE.md](PILOT_LOG_TEMPLATE.md) 记录匿名编号和拒绝原因，不保存真实业务数据。

## 通过门槛

至少 2 个陌生用户各自运行 2 个真实批次；第二批基本独立；至少 1 个发现本来会造成真实返工、退回或解释成本；至少 1 人愿意继续下一批。只有达到这个门槛，才考虑拖拽界面、规则模板或团队报告。

先达到 2 个真实试点就停止扩招；否则在 7 天窗口内继续定向触达，最多记录约 8–10 次有效交流。这是研究预算，不是统计学结论。

## 停止条件

用户只需要 Open Data Editor 等单表工具、没有跨文件问题、没有重复批次、规则需要复杂业务语义，或首次运行后不再使用：停止，不继续增加功能。

如果真实价值已经出现，但第二批被一个明确的启动阻力卡住，只修这个阻力并重跑；不借机增加新校验规则、拖拽 UI、正式安装器、内嵌运行时或 PDF/Excel 导出。
